---
description: Sharpen an idea by interview, updating CONTEXT.md and ADRs as the domain model crystallizes.
---

Use the `grill-with-docs` skill for this request. Read
`.claude/skills/grill-with-docs/SKILL.md` and follow its workflow exactly:
run a `/grilling` session with the `/domain-modeling` skill, updating
`CONTEXT.md` and `docs/adr/` as the domain model crystallizes, and stop when
the user has a clear enough idea to proceed to spec, tickets, or
implementation.

## User request

$ARGUMENTS