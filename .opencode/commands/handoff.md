# handoff — Prepare session summary for next session

Generates a structured summary of current task state, pending decisions,
and next steps. Writes to `SESSION.md` in the repo root.

## Usage

/handoff
