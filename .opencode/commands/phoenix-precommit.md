# precommit — Run Phoenix precommit checks

Run `mix precommit` from the project root.

If the alias is missing, say that plainly and stop instead of guessing at a replacement command. Summarize any failures with the relevant file paths.

## Usage

/precommit
