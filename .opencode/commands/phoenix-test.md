# test — Run the Phoenix test suite

Run `mix test` from the project root.

If the user named a file, directory, or test filter, run the narrowest matching `mix test` command first. Summarize failures with file paths and the next likely fix.

## Usage

/test
