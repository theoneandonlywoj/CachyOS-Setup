# Reflection Artifacts

Each `/reflect` run creates a timestamped directory containing its immutable snapshot, redacted batch findings, failures, and final proposal artifacts. See [the workflow](./REFLECT_WORKFLOW.md) and [configuration reference](./CONFIGURATION.md).
